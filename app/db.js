'use strict';

var log = require('./modules/log'),
  constants = require('./constants')(),
  db,
  dbConnected;

if (!db) {
  db = true; // temp
}

// ----------------
// public functions
// ----------------

// ---------
// interface
// ---------

exports = module.exports = {

};
